[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "csv2json"
version = "0.1.0"
description = "A command-line tool for converting CSV files to JSON."
requires-python = ">=3.10"

[project.scripts]
csv2json = "csv2json.cli:main"